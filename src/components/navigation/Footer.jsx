import { connect } from 'react-redux'

function Footer() {
    return(
        <nav>hooter</nav>)    
}

const mapStateToProps = () => ({ });
  

export default connect(mapStateToProps,{
 })( Footer);